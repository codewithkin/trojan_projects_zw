{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run dist/index.mjs"
  },
  "dependencies": {
    "@trojan_projects_zw/db": "workspace:*",
    "@trojan_projects_zw/env": "workspace:*",
    "@types/ws": "^8.18.1",
    "dotenv": "catalog:",
    "hono": "^4.8.2",
    "ws": "^8.19.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@trojan_projects_zw/config": "workspace:*",
    "@types/bun": "catalog:",
    "tsdown": "^0.16.5",
    "typescript": "catalog:"
  }
}
